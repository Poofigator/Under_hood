server {

    listen 80;

    location / {

        proxy_pass http://web:8000;

        location ~ /api/car/ {
            limit_except POST {
                allow 127.0.0.1;
                deny  all;
            }
        }
    }

    location /static/ {
        alias /static/;
    }
}
